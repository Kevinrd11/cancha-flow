import type { LucideIcon } from "lucide-react";

export function StatCard({ label, value, helper, icon: Icon }: { label: string; value: string; helper?: string; icon: LucideIcon }) { return <article className="rounded-2xl border border-line bg-white p-5"><span className="grid size-9 place-items-center rounded-xl bg-emerald-50 text-green"><Icon size={18} /></span><p className="mt-5 text-sm text-slate-500">{label}</p><p className="mt-1 text-2xl font-bold tracking-[-.025em] text-navy">{value}</p>{helper && <p className="mt-1 text-xs text-slate-400">{helper}</p>}</article>; }
export function EmptyState({ title, description, icon: Icon }: { title: string; description: string; icon: LucideIcon }) { return <div className="p-12 text-center"><span className="mx-auto grid size-12 place-items-center rounded-2xl bg-slate-100 text-slate-400"><Icon size={22} /></span><p className="mt-4 font-bold text-navy">{title}</p><p className="mx-auto mt-1 max-w-md text-sm text-slate-500">{description}</p></div>; }
